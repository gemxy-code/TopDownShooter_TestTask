using DG.Tweening;
using UnityEngine;

public class RotateAnimation : MonoBehaviour
{
    /*
    [SerializeField] private float _rotateTime = 0;

    private Tween _rotateTween;

   private void OnEnable()
    {
        transform.DORotate(new Vector3(0, 360, 0), _rotateTime, RotateMode.FastBeyond360)
            .SetLoops(-1, LoopType.Restart)
            .SetEase(Ease.Linear);
    }

    private void OnDisable()
    {
        _rotateTween.Kill();
    }
    */
}
